/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'voyager-neon.tech',
      'www.voyager-neon.tech',
      'voyager-neon.netlify.app',
    ],
  },
}

module.exports = nextConfig
